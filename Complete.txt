1. таблица "users", поля name,mail,pass имеют слишком большую длину, из-за этого может быть лишняя растрата ресурсов оборудования. Рекомендация:
 `id` int(11) NOT NULL,
  `name` varchar(30) NOT NULL,
  `mail` varchar(50) NOT NULL,
  `pass` varchar(30) NOT NULL
  
2. Bmw.php - мелкие изменения в синтаксисе
3. В регистрации и логинпейдже необходимо использовать reCapcha для защиты от ботов и предотварщения забивания ими базы рандомными данными. (Необходим домен)
https://www.google.com/recaptcha/admin
https://youtu.be/hGkeMfKwTgs

4. index.php
Пароль должен записываться в базу только после шифрования( рекомендую - $Salt = "0x".md5($Login.$Pass); , далее сравнивать данные с солью )
1) Нету проверку на пустые строки
2) Нету проверки на неккоректность ввода(опасность sql иньекции)
3) Не работает проверка повтора пароля в клиент части(ниже код с фиксом)
4) Описаны проверки на максимальную длинну, откоректированные под  пункт 1.

if (empty($username) || empty($password) || empty($password2) || empty($email))
            {
                echo "Поля пустые";
            }    
        elseif (preg_match("[^0-9a-zA-Z_-]", $username, $Txt))
            {
                echo "Логин неккоректного формата";
            }            
        elseif (preg_match("[^0-9a-zA-Z_-]", $password, $Txt))
            {
                echo "Пароль неккоректного формата";    
            }       
        elseif (preg_match("[^0-9a-zA-Z_-]", $password2, $Txt))
            {
                echo "Пароль неккоректного формата";    
            }
        elseif (StrPos('\'', $email))
            {
                echo "Почта неккоректного формата";    
            }    
        else
            {
                elseif ((StrLen($username) < 8) or (StrLen($username) > 30))       
            {
                echo "Логин должен быть от 8 до 30 символов";
            }         
        elseif ((StrLen($password) < 8) or (StrLen($password) > 30))     
            {
                echo "Пароль должен быть от 8 до 30 символов";
            }           
        elseif ((StrLen($password2) < 8) or (StrLen($password2) > 30)) 
            {
                echo "Пароль должен быть от 8 до 30 символов";
            }           
        elseif ((StrLen($email) < 10) or (StrLen($email) > 50)) 
            {
                echo "Почта должна быть от 10 до 50 символов";
            }      
        elseif ($password != $password2)
            {
                echo "Пароли не совпадают";
            }
			
5. Внутри директории необходим .htaccess для файлов конфига и корректные права на файлы.
6. Все проверки в файле index.php на строках 155-178 лучше убрать и переделать на мотив, описанный выше, так как они светятся в открытом виде.